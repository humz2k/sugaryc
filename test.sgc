#include <stdlib.sgh>
#include <string.sgh>
#include <ndarray.sgh>
#include <math.sgh>

int main(){
    int_array a = make_int_array(2,5);
    println(a.dims(),a.dims()[0],a.dims()[1]);
    return 0;
}