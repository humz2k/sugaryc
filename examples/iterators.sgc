#include <stdlib.sgh>
#include <range.sgh>

int main(){

    foreach(auto i in range(10)){
        print(i);
    }
    println();

    foreach(auto i in range(1,10,2)){
        println(i);
        foreach(auto j in range(1,i)){
            print(j);
        }
    }
    println();

    return 0;
}